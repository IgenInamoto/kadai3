<div class="mt-3">
  <%= form_with model: [ book, book_comment ], local: false do |f| %>
    <%#= render 'partial/error_messages', object: f.object %>
    <%= f.text_area :comment, placeholder: "", class: "w-100" %><br>
    <%= f.submit "送信" %>
  <% end %>
</div>